services:
  frontend:
    build: .
    container_name: ledger-frontend-dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules 
    environment:
      - VITE_API_URL=http://localhost:5000/api/ 
    stdin_open: true
    tty: true